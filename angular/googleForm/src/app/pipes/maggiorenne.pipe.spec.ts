import { MaggiorennePipe } from './maggiorenne.pipe';

describe('MaggiorennePipe', () => {
  it('create an instance', () => {
    const pipe = new MaggiorennePipe();
    expect(pipe).toBeTruthy();
  });
});
