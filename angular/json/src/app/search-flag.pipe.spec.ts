import { SearchFlagPipe } from './search-flag.pipe';

describe('SearchFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
